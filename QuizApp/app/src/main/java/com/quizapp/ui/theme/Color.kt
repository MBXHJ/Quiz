package com.quizapp.ui.theme

import androidx.compose.ui.graphics.Color

// ══════════════════════════════════════════════
// Trivia & Quiz Game Palette (from UI UX Pro Max)
// Quiz blue + gold accent, professional-grade contrast
// ══════════════════════════════════════════════

// ── Primary: Blue #2563EB ──
val Primary = Color(0xFF2563EB)
val PrimaryDark = Color(0xFF1D4ED8)
val PrimaryLight = Color(0xFF60A5FA)
val PrimaryContainer = Color(0xFFDBEAFE)   // blue-100
val OnPrimaryContainer = Color(0xFF1E3A5F)  // blue-900 tone

// ── Secondary: Violet #7C3AED ──
val Secondary = Color(0xFF7C3AED)
val SecondaryContainer = Color(0xFFEDE9FE)  // violet-100
val OnSecondaryContainer = Color(0xFF4C1D95)

// ── Accent / CTA: Gold #F59E0B ──
val AccentGold = Color(0xFFF59E0B)
val AccentGoldContainer = Color(0xFFFEF3C7)

// ── Surfaces ──
val Background = Color(0xFFF0F4FF)         // very light blue tint (distinct from white)
val SurfaceWhite = Color(0xFFFFFFFF)
val SurfaceVariant = Color(0xFFEEEEEE)     // ~grey-200 – clearly visible card bg
val SurfaceMuted = Color(0xFFF5F5F5)       // almost white grey

// ── Text ──
val TextPrimary = Color(0xFF0F172A)        // slate-900
val TextSecondary = Color(0xFF64748B)      // slate-500
val TextTertiary = Color(0xFF94A3B8)       // slate-400

// ── Borders ──
val Border = Color(0xFFE4ECFC)             // quiz blue border
val BorderLight = Color(0xFFF1F5FD)

// ── Status ──
val CorrectGreen = Color(0xFF10B981)
val CorrectGreenBg = Color(0xFFECFDF5)
val WrongRed = Color(0xFFEF4444)
val WrongRedBg = Color(0xFFFEF2F2)
val WarningOrange = Color(0xFFF59E0B)
val WarningOrangeBg = Color(0xFFFFFBEB)

// ── Gradient ──
val GradientStart = Color(0xFF2563EB)       // blue
val GradientMid = Color(0xFF6366F1)         // indigo
val GradientEnd = Color(0xFF7C3AED)         // violet

// ── Dark mode ──
val DarkBackground = Color(0xFF0F172A)
val DarkSurface = Color(0xFF1E293B)
val DarkCard = Color(0xFF334155)          // slate-700 — clearly visible as option bg
val DarkTextPrimary = Color(0xFFF1F5F9)    // slate-100 — near-white text
val DarkTextSecondary = Color(0xFFCBD5E1)  // slate-300 — clearly readable secondary text
val DarkBorder = Color(0xFF64748B)
